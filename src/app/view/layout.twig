<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <link rel="icon" href="/favicon.ico">
    <title>{{ title }}</title>
    <meta name="keyword" content="{{ keyword }}">
    <meta name="description" content="{{ description }}">
    {% for css in cssFiles %}
    <link rel="stylesheet" href="/{{ vueStatic.css }}/{{ css }}">
    {% endfor %}
  </head>
  <body>
    <noscript>
      <strong>We're sorry but ithome2019ironman-hybrid doesn't work properly without JavaScript enabled. Please enable it to continue.</strong>
    </noscript>
    <div id="app"></div>
    {% for js in jsFiles %}
    <script src="/{{ vueStatic.js }}/{{ js }}"></script>
    {% endfor %}
  </body>
</html>
